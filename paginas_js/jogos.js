

function rodar (correto, errado, mensagem1, mensagem2, mickeyp){
    let = resposta (prompt("digite sua resposta").toLowerCase())
    if (resposta == correto) {
        window.location.assign('./pagMickeyPt2.html');
    }
}